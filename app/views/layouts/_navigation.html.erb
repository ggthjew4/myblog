<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <% if user_signed_in? %>
          <li><%= button_to("Create new article", new_article_path, method: :get ,:class => "btn btn-primary") %></li>
        <% end %>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li><%= button_to("Logout", destroy_user_session_path, method: :get ,:class => "btn btn-primary") %></li>
        <% else %>
          <li><%= button_to("Sign up", new_user_registration_path, method: :get ,:class => "btn btn-primary") %></li>|
          <li><%= button_to("Login", new_user_session_path, method: :get ,:class => "btn btn-primary") %></li>
      <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>